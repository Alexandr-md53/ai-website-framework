Phase 10 — Product Registry & CLI
C4 scaffold crud template filesystem-only, manifest.json only validate_scaffold -crud suffix (430)
C5.1 registry ProductInfo list_products get_product keep EndpointRegistry (433)
C5.2 enrichment pyproject_name/version/requires_python + lru_cache (435)
C5.3 cli read-only hooks product list/showcase list/info (439)
C5.4 cli version + product info alias + --json (444)
C5.5 api inspect_project export + cli inspect <path> [--json] filesystem-only (447)
